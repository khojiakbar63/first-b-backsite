- npm i dotenv / import dotenv from 'dotenv' / dotenv.config();
- app.use(express.json()) for converting json to javascript object => PUT | POST
- PUT method is mix version of GET(by id) and POST
# first-b-backsite
